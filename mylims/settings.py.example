"""
Django settings for mylims project.

Generated by 'django-admin startproject' using Django 2.1.2.

For more information on this file, see
https://docs.djangoproject.com/en/2.1/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/2.1/ref/settings/
"""

import os

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/2.1/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = ''

SECURE_CONTENT_TYPE_NOSNIFF = True
SECURE_SSL_REDIRECT = True
CSRF_COOKIE_SECURE = True
SESSION_COOKIE_SECURE = True
X_FRAME_OPTIONS = 'DENY'
CSRF_COOKIE_HTTPONLY = True
SESSION_COOKIE_SECURE = True
SESSION_COOKIE_AGE = 259200 # Lower session length to 72 hours from 2 week default

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []
SITE_URL = "https://majora.site"

ADMINS = [('Sam', 'sam@example.org')]

SILENCED_SYSTEM_CHECKS = [
    'mysql.E001' # https://stackoverflow.com/questions/45233362/django-says-that-mysql-does-not-allow-unique-charfields-to-have-a-max-length-2
]

# Application definition

INSTALLED_APPS = [
    'dal',
    'dal_select2',
    'polymorphic',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'django.contrib.humanize',
    #'lims',
    'majora2',
    'django.contrib.admin',
    'tatl',
    'deku',
    'django_slack',
    'crispy_forms',
    'django_celery_results',
    'django_otp',
    'django_otp.plugins.otp_static',
    'django_otp.plugins.otp_totp',
    'two_factor',
    'rest_framework',
    'rest_framework.authtoken',
    'markdownify',
    'oauth2_provider',
    'corsheaders',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'corsheaders.middleware.CorsMiddleware',
    'django_otp.middleware.OTPMiddleware',
    'tatl.middleware.TempOAuth2TokenMiddleware',
    'tatl.middleware.TatlRequestLogMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'mylims.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'majora2.context_processors.majora_version',
            ],
            'loaders': [
                'django.template.loaders.app_directories.Loader',
            ]
        },
    },
]

WSGI_APPLICATION = 'mylims.wsgi.application'


# Database
# https://docs.djangoproject.com/en/2.1/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
    }
}


# Password validation
# https://docs.djangoproject.com/en/2.1/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/2.1/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/2.1/howto/static-files/

STATIC_URL = '/static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'static/')

SLACK_TOKEN = ''
SLACK_CHANNEL = '#majora'
SLACK_CHANNEL_TATL = '#tatl'
SLACK_AS_USER = True
SLACK_BACKEND = 'django_slack.backends.UrllibBackend'

LOGIN_URL = 'two_factor:login'
LOGIN_REDIRECT_URL = '/accounts/profile/'

API_KEY = ""

INSTANCE_NAME = ""
INSTANCE_MAINTAINER = ""
INSTANCE_COLOUR = "#292b2c"

CRISPY_TEMPLATE_PACK = 'bootstrap4'

USER_PREFIX = ""
USER_LEN_BEFORE_NUM = 30

EMAIL_SUBJECT_PREFIX = "[Majora]"
#DEFAULT_FROM_EMAIL = "majora@"
#SERVER_EMAIL = "majora@"

CELERY_CACHE_BACKEND = 'django-cache'
CELERY_BROKER_URL = ''

CELERY_RESULT_BACKEND = 'django-db' # for django backend
#CELERY_RESULT_BACKEND = 's3'

CELERY_S3_ENDPOINT_URL = ""
CELERY_S3_ACCESS_KEY_ID = ""
CELERY_S3_SECRET_ACCESS_KEY = ""
CELERY_S3_BUCKET = ""

REST_FRAMEWORK = {
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 10,

    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticated',
    ],

    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.SessionAuthentication',
        'oauth2_provider.contrib.rest_framework.OAuth2Authentication',
    ],

    'DEFAULT_RENDERER_CLASSES': [
        'rest_framework.renderers.BrowsableAPIRenderer',
        'rest_framework.renderers.JSONRenderer',
        'rest_framework_csv.renderers.CSVRenderer',
    ],
    'TEST_REQUEST_DEFAULT_FORMAT': 'json',
}

MARKDOWNIFY_WHITELIST_TAGS = [
    'a',
    'abbr',
    'acronym',
    'b',
    'blockquote',
    'code',
    'em',
    'h1',
    'h2',
    'h3',
    'h4',
    'h5',
    'h6',
    'i',
    'li',
    'ol',
    'p',
    'strong',
    'ul'
]

LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'verbose': {
            'format': 'majora[{levelname}]: {module} {message}',
            'style': '{',
        },
    },
    'handlers': {
        'syslog': {
            'level': 'INFO',
            'class': 'logging.handlers.SysLogHandler',
            'facility': 'local3',
            'address': '/dev/log',
            'formatter': 'verbose'
       },
    },
    'loggers': {
        'majora': {
            'handlers': ['syslog'],
            'format': 'verbose',
            'level': 'INFO',
            'propagate': True,
        },
    },
}

# There is probably a proper way to make sure these are set to import in majora2/models.py but whatever
# https://github.com/jazzband/django-oauth-toolkit/issues/240#issuecomment-403928239
OAUTH2_PROVIDER_ACCESS_TOKEN_MODEL = 'oauth2_provider.AccessToken'
OAUTH2_PROVIDER_APPLICATION_MODEL = 'oauth2_provider.Application'
OAUTH2_PROVIDER_REFRESH_TOKEN_MODEL = 'oauth2_provider.RefreshToken'

OAUTH2_PROVIDER = {
    'OAUTH2_VALIDATOR_CLASS': 'tatl.oauth2.ApplicationSpecificOAuth2Validator',
    'SCOPES_BACKEND_CLASS': 'tatl.oauth2.PermissionScopes',
    'ALLOWED_REDIRECT_URI_SCHEMES': ["https"],
    'REQUEST_APPROVAL_PROMPT': 'auto',
    'OAUTH2_BACKEND_CLASS': 'tatl.oauth2.TatlOAuthLibCore',
}
OAUTH2_PROVIDER_APPLICATION_MODEL='tatl.OAuth2CodeOnlyApplication'

# Enable CORS headers on API and OAuth endpoints
CORS_ALLOWED_ORIGINS = []
CORS_ALLOWED_ORIGIN_REGEXES = []
CORS_ALLOW_ALL_ORIGINS = True # This is dangerous but we limit the CORS URLs to the token interface below
CORS_URLS_REGEX = r'^/o/token/$'
CORS_URLS_ALLOW_ALL_REGEX = r'^/o/token/$'

# CONTACTS
MAJORA_ACCOUNT_MAIL = ""
